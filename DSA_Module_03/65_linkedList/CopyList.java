public class CopyList {
    public static void main(String[] args) {
        
    }
    
}

/*
 * you are given a linked list A
 * each node in the linked list contains two pointers: a next pointer and a random pointer
 * the next pointer points to the next node in the list 
 * the random pointer can point to any node in the list or it can be null
 * your task is to create a deep copy of the linked list A 
 * the copied list should be a completely seperate linked list from the original list but the same node values and random pointer connections as the original list 
 * you should create a new linked list B where each node B has the same value as the curresponding node in A
 * the next and random pointers of each node in B should point the curresponding nodes in B (rether than A)
 */
